
  # Create UX Wireframe Sketch

  This is a code bundle for Create UX Wireframe Sketch. The original project is available at https://www.figma.com/design/aNdcpTtf2xCCGzMxybLYDQ/Create-UX-Wireframe-Sketch.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  The app now launches directly to the React Candidate Ranking Dashboard at `/` (also available at `/dashboard`).
  
